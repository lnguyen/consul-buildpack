#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

build_dir=$1
cache_dir=$2

compile_buildpack_dir=$(cd $(dirname $0); cd ..; pwd)
compile_buildpack_bin=$compile_buildpack_dir/bin

cp $compile_buildpack_bin/consul.sh .
